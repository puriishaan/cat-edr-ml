# pip fallback — prefer conda (environment.yml) for eccodes/cartopy system deps
# Install with: pip install -r requirements.txt

# Data handling
xarray>=2024.1
cfgrib>=0.9.14
netCDF4>=1.6
pandas>=2.1
numpy>=1.26
pyarrow>=15.0
gcsfs
zarr

# Atmospheric science
metpy>=1.6

# Scientific computing
scipy>=1.12
scikit-learn>=1.4
xgboost>=2.0

# Satellite / cloud data
s3fs>=2024.1
satpy>=0.47
goes2go>=2024.1

# ERA5 access
cdsapi>=0.7

# Geospatial
pyproj

# HTTP / async
requests
aiohttp

# Visualization
matplotlib>=3.8
# NOTE: cartopy requires system libs (GEOS, PROJ) — use conda or install them
#       manually before running: pip install cartopy
cartopy>=0.22

# Jupyter
jupyter>=1.0
ipykernel>=6.29
ipywidgets>=8.1

# Utilities
tqdm>=4.66
pyyaml>=6.0

# Deep learning (physics-informed CNN) + hyperparameter search
# CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
optuna>=3.6
